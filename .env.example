# Root-level env example — copy to apps/api/.env and apps/web/.env
# See ENVIRONMENT_SETUP.md for full details

# Database
POSTGRES_USER=stocksense
POSTGRES_PASSWORD=stocksense_dev
POSTGRES_DB=stocksense
